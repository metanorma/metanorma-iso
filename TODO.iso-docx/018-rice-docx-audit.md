# 018 — Rice DOCX Audit: New Adapter vs Reference Output

## Reference file
`data/iso-rice-sample-output-repaired.docx` — generated by the OLD isodoc HTML→DOCX pipeline,
then opened and re-saved by Word. This is the baseline for comparison.

## New output
Generated by `IsoDoc::Iso::Docx::Adapter` from `spec/examples/rice.presentation.xml`.
Uses the ISO DIS template (`data/iso-dis/template.docx`) with proper style mapping.

## Summary Statistics

| Metric | New Output | Reference |
|--------|-----------|-----------|
| File size | 437 KB | 45 KB |
| Paragraphs | 294 | 303 |
| Tables | 2 | 1 |
| Hyperlinks | 79 (69 internal, 10 external) | 60 (all internal) |
| Footnote refs | 22 | 10 |
| Images | 2 (embedded) | 0 (text placeholders) |
| Page breaks | 5 | 0 |
| Body text styles | BodyText, ForewordText, etc. | (none / default) |
| Annex headings | a2, a3 (template styles) | Heading1, a2 (mixed) |
| Numbering numIds | 5 (dash), 8 (decimal) | 10 (single numId for all) |

## Category 1: New Output is CORRECT (reference was wrong)

These differences are intentional improvements. The new adapter uses the ISO DIS template
correctly, while the old pipeline bypassed template styles entirely.

### 1a. Paragraph styles assigned from template ✓
- Old: All body text = `(none)` / default style
- New: `BodyText` for body text, `ForewordText` for foreword, `Notice` for admonitions
- Correct: Template defines these styles; adapter maps to them via `style_mapping.yml`

### 1b. Annex heading styles ✓
- Old: A.1 = Heading1, A.2.1 = a2 (inconsistent)
- New: A.1 = a2, A.2.1 = a3 (consistent with template's a2–a6 hierarchy)
- Correct: The ISO template defines `ANNEX` → `a2` → `a3` → `a4` → `a5` → `a6` for
  annex heading levels. This is the standard ISO convention.

### 1c. Table cell styles ✓
- Old: All cells = `(none)`
- New: `Tableheader`, `Tablebody` styles from template
- Correct: Template defines these for table cells

### 1d. Numbering ✓
- Old: All lists use numId 10 (single numbering definition)
- New: dash_list=5, decimal_list=8 (semantically correct numbering)
- Correct: Different list types should use different numbering definitions

### 1e. Images embedded ✓
- Old: Base64 text placeholders like `[Image: wUZlqg3R...]`
- New: Actual embedded images via `a:blip` with `r:embed` references
- Correct: Images should be proper embedded objects

### 1f. External hyperlinks ✓
- Old: No hyperlinks (URLs stripped entirely)
- New: External URL hyperlinks with `r:id` referencing relationships
- Correct: URLs in the source should become clickable hyperlinks

### 1g. Admonitions ✓
- Old: WARNING/CAUTION = `(none)` style
- New: `Notice` style (the ISO template style for admonitions)
- Correct: Template defines `Notice` for this purpose

### 1h. No bibliography duplication ✓
- Old: Bibliography entries rendered twice (34 entries instead of 17)
- New: 17 unique bibliography entries
- Correct: Our adapter renders each bibitem once

## Category 2: Bugs to Fix

### 2a. Hyperlink text duplication (mailto: URL)
**Location**: Introduction, patent holder contact info (paragraph 19)
**Problem**: Text shows `mailto:gehf@vacheequipment.ficmailto:gehf@vacheequipment.fic`
The `mailto:` URI appears twice — once as the link target and once as display text,
concatenated without separator.
**Root cause**: Inline renderer renders both the email address text and the mailto: URI.
**Fix**: When rendering a hyperlink with `mailto:` scheme, use only the email address
as display text, not `mailto:address`.

### 2b. Cross-reference text concatenation
**Location**: Various term definitions and body text
**Problem**: Cross-references concatenate with adjacent text without separator:
`"3.1husked rice"` instead of `"3.1 husked rice"` or separate elements.
Also: `").B.3.2B.3.1"` — reference labels concatenated without space.
**Root cause**: Inline renderer may not insert space between xref and following text,
or the presentation XML has them concatenated.
**Fix**: Check if the presentation XML has the concatenation or if our renderer
introduces it. If the XML is clean, fix the renderer to add spacing.

### 2c. Empty paragraphs in terms
**Location**: Term definitions (e.g., paragraphs 36, 44, 51)
**Problem**: Extra empty `[BodyText]` paragraphs appear inside term definitions.
The reference also has these as `(none)` paragraphs — they may come from the
XML structure (e.g., empty `<p>` elements).
**Analysis**: These may be correct if the XML contains empty paragraphs.
Need to verify against presentation XML before deciding to fix.

### 2d. References section heading uses wrong depth
**Location**: `visit_references_section` (line 269)
**Problem**: `@resolver.heading_style(2)` always uses depth 2, but normative
references should use Heading1 (it's clause 2). Bibliography heading
should also use the correct depth.
**Fix**: Use the section's actual depth from context, not hardcoded 2.

### 2e. Missing "Normative references" heading in body text
**Problem**: The reference has no separate heading for "2 Normative references"
in body text — the references are listed after the heading without a section
break. Our output renders them but the heading is missing from the main body
comparison (both outputs have similar structure here).
**Analysis**: Need to verify both outputs have the heading rendered.

## Category 3: Structural Differences (Acceptable)

### 3a. File size difference (437 KB vs 45 KB)
The new output is much larger because:
- It uses the full ISO DIS template (styles.xml = 156 KB vs 129 KB)
- Images are embedded (media/ directory)
- Numbering.xml is larger (26 KB vs 21 KB) — template numbering preserved
- webSettings.xml is 40 KB (template webSettings preserved) vs 1 KB

### 3b. Page breaks vs section breaks
- New: 5 explicit page breaks
- Ref: No page breaks
- Analysis: Page breaks are needed for annex starts. The old pipeline may
  have used section properties instead.

### 3c. Relationship IDs
- New: rId1–rId25 (images, hyperlinks, headers, footers)
- Ref: rId1–rId10 (infrastructure only)
- Analysis: More rels because we have images, hyperlinks, multiple headers/footers

## Action Items

1. Fix 2a: Hyperlink text duplication (mailto:)
2. Fix 2b: Cross-reference text concatenation
3. Fix 2c: Verify empty paragraphs in terms (may be correct)
4. Fix 2d: References section heading depth
5. Generate updated DOCX and re-verify
