template:
  # skip standardidentifier, it is inserted in front of formattedref within metanorma
  standard: "<em><span_class='stddocTitle'>{{ title }}</span></em> ,_{{ edition | capitalize_first }} ._{{ extent }}." 
