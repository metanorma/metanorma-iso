# frozen_string_literal: true

module Metanorma
  module Iso::Document
    module Terms
      # Note to a term entry: the shared standoc mapping (block
      # children + rendering channels), inherited verbatim.
      class TermNote < Metanorma::Standoc::Document::Terms::TermNote
      end
    end
  end
end
